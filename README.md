### Phygital Deployment CLI TOOL

the app engine folder allows the application to be deployed via App Engine

the compute engine folder contains the CLI tool > see the readme.adoc in that folder for more infor
